export enum Cover {
   ABSENT = 'absent',
   HALF = 'half',
   THREE_QUARTERS = 'three quarters',
}
export enum CoverOnRu {
   ABSENT = 'отсутствует',
   HALF = 'половина',
   THREE_QUARTERS = 'три четвертых',
}
